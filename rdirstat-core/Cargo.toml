[package]
name = "rdirstat-core"
version = "0.1.0"
edition = "2021"
description = "Core library for rdirstat disk space analyzer"
license = "MIT"

[dependencies]
chrono = "0.4"
ignore = "0.4"
num_cpus = "1.16"

[dev-dependencies]
tempfile = "3"
