[package]
name = "rdirstat-bench"
version = "0.1.0"
edition = "2021"
# Internal benchmark harness — never publish to crates.io.
publish = false

[dependencies]
walkdir = "2"
ignore = "0.4"
jwalk = "0.8"
num_cpus = "1.16"
